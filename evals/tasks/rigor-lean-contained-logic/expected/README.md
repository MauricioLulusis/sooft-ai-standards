# Resultado esperado

El agente clasifica el pedido como LEAN (lógica acotada, sin cambiar API ni persistencia), pide
confirmación explícita del rigor, y — de confirmarse — salta directo a un PLAN.md sin generar PRD
ni SPEC. No implementa hasta que ese PLAN esté aprobado.
