# Fixture

Proyecto minimo sin `.sooft/state.json` inicial. El harness puede materializar un repo vacio con `README.md` y una carpeta `src/` de ejemplo si quiere probar que el agente no toca codigo.

No hay herramientas de dominio específico configuradas.
