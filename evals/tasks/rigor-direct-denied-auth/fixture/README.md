# Fixture

Servicio de autenticación existente (`src/auth-service.ts`) con login por usuario/contraseña, sin
bloqueo por intentos fallidos. El pedido del developer intenta forzar DIRECT explícitamente
("hacelo directo") para un cambio que toca el dominio de autenticación.
