# Security OFB eval fixture

Frontend Next.js + React + TypeScript. El flujo sensible de desafio vive en `contexts/Challenge/ChallengeContext.tsx` y las llamadas HTTP salen por `lib/services/apiClient.ts`.
