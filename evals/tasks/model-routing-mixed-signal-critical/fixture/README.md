# Fixture

`src/auth-middleware.ts` existente: función `requireAuth` con un typo en el mensaje de log
("authentification" en vez de "authentication"). El ticket lo describe como un cambio trivial de
un typo, pero el archivo tocado es el middleware de autenticación.
