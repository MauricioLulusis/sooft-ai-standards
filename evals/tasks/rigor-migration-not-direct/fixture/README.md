# Fixture

Servicio de pedidos existente (`package.json` con `engines.node` en 16.x). El pedido usa la palabra
"directo" para describir la simplicidad esperada, pero se trata de un upgrade de runtime — trabajo de
migración, fuera del alcance de la clasificación DIRECT/LEAN/FULL de `sooft-development`.
