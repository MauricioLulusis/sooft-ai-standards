# Resultado esperado

El agente detecta que el pedido es ambiguo (no dice si el problema es performance, relevancia,
índices o UX) y pide precisiones antes de clasificar rigor o escribir código. Ninguna clasificación
DIRECT es válida mientras el alcance no esté claro.
