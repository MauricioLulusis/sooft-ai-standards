# Fixture

Repositorio sin `.sooft/`. El prompt confirma el issue tracker disponible y Confluence ausente.

El harness puede incluir archivos de stack (`package.json`, `pom.xml`, `pyproject.toml`) para evaluar inferencia.
